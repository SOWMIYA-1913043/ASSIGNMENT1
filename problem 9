#include <stdio.h>

int main()
{
  int a, b,sum=1000;

  for (a = 2; a <sum; a++) {
    for (b = 1; b < a; b++) {
      int c = (sum - (a + b));
      if (a*a + b*b == c*c) {
        printf("%d\n", a * b * c);
      }
    }
  }
  return 0;
}
